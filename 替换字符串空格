注意字符串是不可变对象


class Solution:
    def replaceSpace(self, s: str) -> str:
        d = ''
        for c in s :
            if c == " ":
                c = "%20"
                d = d+c
            else:d = d+c
        return d
